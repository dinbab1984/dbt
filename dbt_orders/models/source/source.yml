
version: 2

sources:
  - name: raw
    schema: json
    tables:
      - name: flight_data
        identifier: "`file:///data/flight-data/json`"
        qouting:
          identifier: true
  - name: reference
    schema: orders_reference
    tables:
     - name: ref_data1